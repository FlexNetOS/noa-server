apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "noa-server.fullname" . }}-mcp-data
  labels:
    {{- include "noa-server.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: fast-ssd
