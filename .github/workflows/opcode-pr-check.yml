name: Opcode - PR Checks (bun run check)

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - 'opcode/**'
      - '.github/workflows/opcode-pr-check.yml'

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: pr-check-${{ github.workflow }}-${{ github.event.pull_request.head.sha || github.sha }}
  cancel-in-progress: true

jobs:
  check:
    name: bun run check
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./opcode

    steps:
      - name: Checkout repository
        uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332

      - name: Set up Bun
        uses: oven-sh/setup-bun@4bc047ad259df6fc24a6c9b0f9a0cb08cf17fbe5
        with:
          bun-version: latest

      - name: Cache Bun dependencies
        uses: actions/cache@6849a6489940f00c2f30c0fb92c6274307ccb58a
        with:
          path: |
            ~/.bun/install/cache
            node_modules
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Install JS/TS dependencies
        run: bun install --frozen-lockfile

      - name: Set up Rust (stable)
        uses: dtolnay/rust-toolchain@6d9817901c499d6b02debbb57edb38d33daa680b

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@23bce2519186d4e5285a58e8e37ed955ad904019
        with:
          workspaces: |
            opcode/src-tauri -> opcode/src-tauri/target
          cache-on-failure: true

      - name: Run checks
        run: bun run check
